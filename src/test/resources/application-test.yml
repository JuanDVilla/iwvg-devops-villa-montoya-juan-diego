logging:
  level:
    es.upm.miw: DEBUG

spring:
  datasource:
    url: jdbc:h2:mem:devopsDb;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true